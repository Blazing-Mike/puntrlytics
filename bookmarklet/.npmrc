# Overrides the user-level `allow-scripts` value (e.g. in ~/.npmrc), which
# npm 11.17 misreads as a CLI flag and rejects in project-scoped installs with
# EALLOWSCRIPTS. Script approvals for this package live in package.json's
# `allowScripts` field instead, which is the documented location.
allow-scripts=
